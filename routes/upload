const express = require('express');
const router = express.Router();
const imageController = require('../controllers/imageController.js');
const uploadService = require('../services/multer.service.js');

router.post('/', uploadService.fileValidation, uploadService.upload, imageController.uploadImage);

module.exports = router;